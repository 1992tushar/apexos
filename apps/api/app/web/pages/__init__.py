"""Page routers, auto-discovered by `app.web.build_web_router`.

Each module here defines a module-level `router: APIRouter` whose handlers call
domain services and render templates from `app/web/templates/<module>/`.
"""
